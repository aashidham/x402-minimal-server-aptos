{
  "name": "x402-minimal-server",
  "version": "1.0.0",
  "description": "Minimal x402 payment-gated API server for Aptos",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env.local server.js",
    "client": "node --env-file=.env.local client.js"
  },
  "dependencies": {
    "@aptos-labs/ts-sdk": "^5.2.1",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  }
}
